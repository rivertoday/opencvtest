#include <QCoreApplication>
#include "samplefunctions.h"

int main(int argc, char *argv[])
{
    //QCoreApplication a(argc, argv);

    //OpenCV如何扫描图像、利用查找表和计时
    //how_to_scan_images(argc, argv);

    //矩阵的掩码操作
    mat_mask_operations(argc, argv);

    //使用OpenCV对两幅图像求和（求混合(blending)）
    //adding_images();

    //改变图像的对比度和亮度
    //basic_linear_transform(argc, argv);

    //用opencv绘图
    //drawing_1();
    //drawing_2();

    //离散傅里叶变换
    //discrete_fourier_transform(argc, argv);

    //输入输出XML和YAML文件
    //file_input_output(argc, argv);

    //与 OpenCV 1 同时使用
    //interoperability_with_OpenCV_1(argc, argv );

    //return a.exec();
}

